{
  "name": "@egov/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.json && node --test 'dist/**/*.test.js'",
    "dev": "tsx src/main.ts",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@egov/adapters-egov-platform": "workspace:*",
    "@egov/adapters-http": "workspace:*",
    "@egov/adapters-messaging": "workspace:*",
    "@egov/adapters-persistence": "workspace:*",
    "@egov/application": "workspace:*",
    "@egov/shared": "workspace:*",
    "ethers": "6.17.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
