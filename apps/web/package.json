{
  "name": "@egov/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node ./src/dev-server.mjs",
    "typecheck": "node -e \"console.log('@egov/web: optional debug shell only — citizen UI is Android (Phase 4)')\"",
    "build": "node -e \"console.log('@egov/web: no product build — Android is the citizen client')\""
  }
}
